import { Model } from 'app/core';


/**
 * Municipality model class.
 */
export class Municipality implements Model {
    id: number;
    name: string;
    created_at: string;
    updated_at: string;
}
