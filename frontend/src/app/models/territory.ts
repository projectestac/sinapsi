import { CnModel } from 'concrete/core';


/**
 * Territory model class.
 */
export class Territory implements CnModel {
    id: number;
    name: string;
    created_at: string;
    updated_at: string;
}
